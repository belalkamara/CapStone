<header class="masthead">
  <div class="overlay"></div>
  <div class="container">
    <div class="row">
      <div class="col-lg-8 col-md-10 mx-auto">
        <div class="post-heading">
          <h1><%= @blog.title %></h1>
          <span class="meta">Posted by
            <a href="#"><%= link_to @blog.user.name.upcase %></a></span>
        </div>
      </div>
    </div>
  </div>
</header>

<article>
  <div class="container">
    <div class="row">
      <div class="col-lg-8 col-md-10 mx-auto">
        <p><%= @blog.body %></p>
      
        <p>Blog Topic: <% @blog.topic.title %></p>

        <%= render 'comments/comment_form' %>

        <%= render @blog.comments %>
        
      </div>
    </div>
    <%= link_to 'Edit', edit_blog_path(@blog) if Blog.blogs_by current_user %> 
    <%= link_to 'Back', blogs_path %>
  </div>
</article>  



